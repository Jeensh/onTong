package com.example.slabdesign.boot.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MyBatisConfig {
    // P1 placeholder. MapperScan is done at @SpringBootApplication.
    // P3 may add SqlSessionFactory customization (TypeHandler, plugin) here.
}
