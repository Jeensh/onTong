package com.example.slabdesign.feature.sd.process.working.action;

import org.springframework.stereotype.Component;

/**
 * sd · working: 설비 제약 검사 액션. std 의 ConstraintEntity 와 도메인 규칙으로 결합.
 * (드라마 DNA "도메인 규칙 연결" 시드 — 코드만 보면 working/std 분리, 규칙으로 연결)
 * P1 placeholder.
 */
@Component
public class SdConstraintCheckAction {
    // P2: check(slabDimension, constraints) → boolean / violation list
}
