package com.example.slabdesign.feature.sd.process.working.action;

import org.springframework.stereotype.Component;

/**
 * sd · working: 목적식 평가 액션 (중량 최대화, 수율 최대화 등).
 * 설정 구동 분기 후보 — objectiveType 키에 따라 다른 알고리즘 경로 (드라마 DNA 시드).
 * P1 placeholder.
 */
@Component
public class SdObjectiveAction {
    // P2: evaluate(slabDimension, objectiveType) → score
}
