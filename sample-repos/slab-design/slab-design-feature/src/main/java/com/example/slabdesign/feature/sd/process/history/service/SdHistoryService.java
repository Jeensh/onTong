package com.example.slabdesign.feature.sd.process.history.service;

import org.springframework.stereotype.Service;

/**
 * sd · history: 이력 흐름 제어. Kafka 이벤트 수신 적재 포함.
 * P1 placeholder.
 */
@Service
public class SdHistoryService {
    // P2: append(historyEvent), search(criteria), ...
}
