package com.example.slabdesign.feature.sd.process.std.action;

import org.springframework.stereotype.Component;

/**
 * sd · std: 기준·룰·제약 마스터 조회·검증 액션.
 * 도메인 규칙 거처 — working/SdConstraintCheckAction 과 도메인적으로 결합.
 * P1 placeholder.
 */
@Component
public class SdStdAction {
    // P2: loadConstraints(plantCode), loadGradeSpec(gradeCode), loadCustomerSpec(customerCode)
}
