package com.example.slabdesign.feature.sd.process.working.action;

import org.springframework.stereotype.Component;

/**
 * sd · working: 슬랩 사이즈 계산 액션 (드라마 DNA 핵심 후보).
 * 주문 스펙 + 제약 + 목적식 → slab 치수.
 * P1 placeholder. P2 에서 본 로직 구현.
 */
@Component
public class SdSlabSizeCalcAction {
    // P2: calcSlabSize(orderSpec, constraints, objective) → SlabDimension
}
