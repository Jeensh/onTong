package com.example.slabdesign.facade.sd.rest.std;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * sd · std: 설계 제약·룰·기준 마스터 조회/관리 API.
 * P1 placeholder.
 * P2 에서 설비 제약, 강종 기준, 고객 스펙 사전, 목적식 정의 CRUD 엔드포인트 추가.
 */
@RestController
@RequestMapping("/api/sd/std")
public class SdStdController {
    // P2: GET /constraints, GET /grade-spec, GET /customer-spec, GET /objective
}
