package com.example.slabdesign.boot.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaConfig {
    // P1 placeholder. Spring Cloud Stream binder is wired via application.yml.
    // P3 may add custom KafkaTemplate / ConsumerFactory beans here.
}
