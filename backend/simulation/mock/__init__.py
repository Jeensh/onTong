"""Parametric mock server for Section 2 API.

Generates dynamic responses based on input parameters (NOT static JSON).
Used during independent development before Section 2 integration.

Toggle: SIMULATION_USE_MOCK=true in .env
"""
