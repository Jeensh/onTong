# Slab 설계 도메인 개념 (자동 생성 — Phase 1 매뉴얼 1차)

> 생성 시각 : 2026-04-25T13:31:29.356406+00:00
> 입력 : `sample-repos/slab-design-real/` (122 java files, 14 @Entity)
> 도구 : 12-analyzer + `JavadocRuleExtractor` + cluster heuristic
> **목적** : Section 2 모델링이 데모 코드를 정확히 분석함을 입증 — 사용자가 검토 후 BusinessTerm 시드로 사용.

이 문서는 **데모 소스 코드를 ground truth 로** 자동 생성됐습니다.
사용자 제공 `sample-repos/slab-design-real/toClaude/sd-tables.md` 와 cross-check 시 차이 있으면 코드 (이 문서) 가 우선.

## 데이터 출처
- JPA `@Entity` / `@Table(name=)` / `@Column(name=, length=, precision=, scale=)` / `@IdClass` / `@Id`
- Javadoc `/** ... */` 안 한국어 룰 문장 (38 건)
- 사용자 제공 도메인 문서 7건 (PROJECT_OVERVIEW.md / sd-tables.md / scenarios.md / ALGORITHM.md / DRAMA_DNA.md / architect.md / slab-design.md) — 검증·보강용

---


## 12 테이블 정의 (코드 ground truth)

코드의 `@Entity` + `@Table` + `@Column` 어노테이션 기반 자동 추출. 사용자 제공 `sd-tables.md` 와 cross-check 후 차이 있으면 보고.

### CAST_SPEC — CastSpecJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.CastSpecJpo`
- Composite PK : `@IdClass(CastSpecPK)`
- ID fields : `cmpCd`, `orgCd`, `smCd`, `castCd`, `machineCd`, `productTypeCd`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `smCd` | `SM_CD` | length=1 | ✓ |
| `castCd` | `CAST_CD` | length=4 | ✓ |
| `machineCd` | `MACHINE_CD` | length=4 | ✓ |
| `productTypeCd` | `PRODUCT_TYPE_CD` | length=4 | ✓ |
| `slabThickness` | `SLAB_THICKNESS` | precision=6, scale=2 |  |
| `widthLow` | `WIDTH_LOW` | precision=10, scale=2 |  |
| `widthHigh` | `WIDTH_HIGH` | precision=10, scale=2 |  |
| `lengthLow` | `LENGTH_LOW` | precision=10, scale=2 |  |
| `lengthHigh` | `LENGTH_HIGH` | precision=10, scale=2 |  |
| `wgtLow` | `WGT_LOW` | precision=10, scale=3 |  |
| `wgtHigh` | `WGT_HIGH` | precision=10, scale=3 |  |

### CUSTOMER_STD — CustomerStdJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.CustomerStdJpo`
- Composite PK : `@IdClass(CustomerStdPK)`
- ID fields : `cmpCd`, `orgCd`, `priority`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `priority` | `PRIORITY` | — | ✓ |
| `productNameCd` | `PRODUCT_NAME_CD` | length=3 |  |
| `customerCd` | `CUSTOMER_CD` | length=10 |  |
| `pkgWgtHigh` | `PKG_WGT_HIGH` | precision=10, scale=3 |  |
| `pkgWgtLow` | `PKG_WGT_LOW` | precision=10, scale=3 |  |

### EDGING_GROUP — EdgingGroupJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.EdgingGroupJpo`
- Composite PK : `@IdClass(EdgingGroupPK)`
- ID fields : `cmpCd`, `orgCd`, `priority`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `priority` | `PRIORITY` | — | ✓ |
| `edgingGroupCd` | `EDGING_GROUP_CD` | length=10 |  |
| `gradeCd` | `GRADE_CD` | length=10 |  |
| `productTypeCd` | `PRODUCT_TYPE_CD` | length=4 |  |
| `customerCd` | `CUSTOMER_CD` | length=10 |  |
| `hrTgtWidthHigh` | `HR_TGT_WIDTH_HIGH` | precision=10, scale=2 |  |
| `hrTgtWidthLow` | `HR_TGT_WIDTH_LOW` | precision=10, scale=2 |  |

### EDGING_SPEC — EdgingSpecJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.EdgingSpecJpo`
- Composite PK : `@IdClass(EdgingSpecPK)`
- ID fields : `cmpCd`, `orgCd`, `edgingGroupCd`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `edgingGroupCd` | `EDGING_GROUP_CD` | length=10 | ✓ |
| `edgingCapLow` | `EDGING_CAP_LOW` | precision=10, scale=2 |  |
| `edgingCapHigh` | `EDGING_CAP_HIGH` | precision=10, scale=2 |  |

### HR_MAX_WGT — HrMaxWgtJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.HrMaxWgtJpo`
- Composite PK : `@IdClass(HrMaxWgtPK)`
- ID fields : `cmpCd`, `orgCd`, `hrCd`, `thickness`, `width`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `hrCd` | `HR_CD` | length=1 | ✓ |
| `thickness` | `THICKNESS` | precision=6, scale=2 | ✓ |
| `width` | `WIDTH` | precision=10, scale=2 | ✓ |
| `maxWgt` | `MAX_WGT` | precision=10, scale=3 |  |

### HR_MIN_WGT — HrMinWgtJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.HrMinWgtJpo`
- Composite PK : `@IdClass(HrMinWgtPK)`
- ID fields : `cmpCd`, `orgCd`, `hrCd`, `thickness`, `width`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `hrCd` | `HR_CD` | length=1 | ✓ |
| `thickness` | `THICKNESS` | precision=6, scale=2 | ✓ |
| `width` | `WIDTH` | precision=10, scale=2 | ✓ |
| `minWgt` | `MIN_WGT` | precision=10, scale=3 |  |

### HR_SPEC — HrSpecJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.HrSpecJpo`
- Composite PK : `@IdClass(HrSpecPK)`
- ID fields : `cmpCd`, `orgCd`, `hrPlantCd`, `productTypeCd`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `hrPlantCd` | `HR_PLANT_CD` | length=1 | ✓ |
| `productTypeCd` | `PRODUCT_TYPE_CD` | length=4 | ✓ |
| `widthLow` | `WIDTH_LOW` | precision=10, scale=2 |  |
| `widthHigh` | `WIDTH_HIGH` | precision=10, scale=2 |  |
| `lengthLow` | `LENGTH_LOW` | precision=10, scale=2 |  |
| `lengthHigh` | `LENGTH_HIGH` | precision=10, scale=2 |  |

### ORDER_CHEMICAL — SDOrderChemicalJpo

- JPO FQN : `com.example.slabdesign.store.sd.working.oracle.jpo.SDOrderChemicalJpo`
- Composite PK : `@IdClass(SDOrderPK)`
- ID fields : `cmpCd`, `orgCd`, `orderNo`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 | ✓ |
| `cMin` | `C_MIN` | precision=8, scale=4 |  |
| `cMax` | `C_MAX` | precision=8, scale=4 |  |
| `cAim` | `C_AIM` | precision=8, scale=4 |  |
| `siMin` | `SI_MIN` | precision=8, scale=4 |  |
| `siMax` | `SI_MAX` | precision=8, scale=4 |  |
| `siAim` | `SI_AIM` | precision=8, scale=4 |  |
| `mnMin` | `MN_MIN` | precision=8, scale=4 |  |
| `mnMax` | `MN_MAX` | precision=8, scale=4 |  |
| `mnAim` | `MN_AIM` | precision=8, scale=4 |  |
| `pMin` | `P_MIN` | precision=8, scale=4 |  |
| `pMax` | `P_MAX` | precision=8, scale=4 |  |
| `pAim` | `P_AIM` | precision=8, scale=4 |  |
| `sMin` | `S_MIN` | precision=8, scale=4 |  |
| `sMax` | `S_MAX` | precision=8, scale=4 |  |
| `sAim` | `S_AIM` | precision=8, scale=4 |  |
| `crMin` | `CR_MIN` | precision=8, scale=4 |  |
| `crMax` | `CR_MAX` | precision=8, scale=4 |  |
| `crAim` | `CR_AIM` | precision=8, scale=4 |  |
| `niMin` | `NI_MIN` | precision=8, scale=4 |  |
| `niMax` | `NI_MAX` | precision=8, scale=4 |  |
| `niAim` | `NI_AIM` | precision=8, scale=4 |  |
| `alMin` | `AL_MIN` | precision=8, scale=4 |  |
| `alMax` | `AL_MAX` | precision=8, scale=4 |  |
| `alAim` | `AL_AIM` | precision=8, scale=4 |  |

### ORDER_OM — SDOrderOmJpo

- JPO FQN : `com.example.slabdesign.store.sd.working.oracle.jpo.SDOrderOmJpo`
- Composite PK : `@IdClass(SDOrderPK)`
- ID fields : `cmpCd`, `orgCd`, `orderNo`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 | ✓ |
| `orderWgtLow` | `ORDER_WGT_LOW` | precision=10, scale=3 |  |
| `orderWgtHigh` | `ORDER_WGT_HIGH` | precision=10, scale=3 |  |
| `orderWidth` | `ORDER_WIDTH` | precision=10, scale=2 |  |
| `orderLength` | `ORDER_LENGTH` | precision=10, scale=2 |  |
| `designPendQty` | `DESIGN_PEND_QTY` | precision=10, scale=3 |  |
| `workDue` | `WORK_DUE` | — |  |
| `pkgWgtLow` | `PKG_WGT_LOW` | precision=10, scale=3 |  |
| `pkgWgtHigh` | `PKG_WGT_HIGH` | precision=10, scale=3 |  |
| `productTypeCd` | `PRODUCT_TYPE_CD` | length=4 |  |
| `customerCd` | `CUSTOMER_CD` | length=10 |  |
| `prodDue` | `PROD_DUE` | — |  |
| `deliveryDue` | `DELIVERY_DUE` | — |  |

### ORDER_OS — SDOrderOsJpo

- JPO FQN : `com.example.slabdesign.store.sd.working.oracle.jpo.SDOrderOsJpo`
- Composite PK : `@IdClass(SDOrderPK)`
- ID fields : `cmpCd`, `orgCd`, `orderNo`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 | ✓ |
| `osProgress` | `OS_PROGRESS` | length=1 |  |
| `closeFlag` | `CLOSE_FLAG` | — |  |
| `stockCode` | `STOCK_CODE` | — |  |
| `designPendQtyHigh` | `DESIGN_PEND_QTY_HIGH` | precision=10, scale=3 |  |
| `designPendQtyLow` | `DESIGN_PEND_QTY_LOW` | precision=10, scale=3 |  |
| `designPendQty` | `DESIGN_PEND_QTY` | precision=10, scale=3 |  |
| `confirmedPlantCd` | `CONFIRMED_PLANT_CD` | length=8 |  |
| `possiblePlantCd` | `POSSIBLE_PLANT_CD` | length=16 |  |
| `smDue` | `SM_DUE` | — |  |
| `hrDue` | `HR_DUE` | — |  |
| `hrfDue` | `HRF_DUE` | — |  |
| `crDue` | `CR_DUE` | — |  |
| `anl1Due` | `ANL1_DUE` | — |  |
| `anl2Due` | `ANL2_DUE` | — |  |
| `galDue` | `GAL_DUE` | — |  |
| `crfDue` | `CRF_DUE` | — |  |

### ORDER_QD — SDOrderQdJpo

- JPO FQN : `com.example.slabdesign.store.sd.working.oracle.jpo.SDOrderQdJpo`
- Composite PK : `@IdClass(SDOrderPK)`
- ID fields : `cmpCd`, `orgCd`, `orderNo`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 | ✓ |
| `gradeCd` | `GRADE_CD` | length=10 |  |
| `hrTgtWidth1` | `HR_TGT_WIDTH_1` | precision=10, scale=2 |  |
| `hrTgtWidth2` | `HR_TGT_WIDTH_2` | precision=10, scale=2 |  |
| `hrTgtWidth3` | `HR_TGT_WIDTH_3` | precision=10, scale=2 |  |
| `hrTgtWidth4` | `HR_TGT_WIDTH_4` | precision=10, scale=2 |  |
| `hrTgtWidth5` | `HR_TGT_WIDTH_5` | precision=10, scale=2 |  |

### SD_PRODUCTIVITY_STD — SdProductivityStdJpo

- JPO FQN : `com.example.slabdesign.store.sd.std.oracle.jpo.SdProductivityStdJpo`
- Composite PK : `@IdClass(SdProductivityStdPK)`
- ID fields : `cmpCd`, `orgCd`, `procCd`, `gradeCd`, `prodKindCd`, `customerCd`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `procCd` | `PROC_CD` | length=4 | ✓ |
| `gradeCd` | `GRADE_CD` | length=10 | ✓ |
| `prodKindCd` | `PROD_KIND_CD` | length=4 | ✓ |
| `customerCd` | `CUSTOMER_CD` | length=10 | ✓ |
| `productivity` | `PRODUCTIVITY` | precision=7, scale=4 |  |

### SLAB_DESIGN_HIST — SlabDesignHistJpo

- JPO FQN : `com.example.slabdesign.store.sd.history.oracle.jpo.SlabDesignHistJpo`
- Composite PK : `@IdClass(SlabDesignHistPK)`
- ID fields : `cmpCd`, `orgCd`, `histId`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `histId` | `HIST_ID` | length=30 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 |  |
| `slabNo` | `SLAB_NO` | length=20 |  |
| `stepNo` | `STEP_NO` | — |  |
| `stepName` | `STEP_NAME` | length=50 |  |
| `errorCode` | `ERROR_CODE` | length=20 |  |
| `snapshot` | `SNAPSHOT` | — |  |
| `eventTime` | `EVENT_TIME` | — |  |

### SLAB_RESULT — SlabResultJpo

- JPO FQN : `com.example.slabdesign.store.sd.working.oracle.jpo.SlabResultJpo`
- Composite PK : `@IdClass(SlabResultPK)`
- ID fields : `cmpCd`, `orgCd`, `slabNo`

| Java 필드 | DB 컬럼 | 타입 메타 | PK |
|---|---|---|---|
| `cmpCd` | `CMP_CD` | length=2 | ✓ |
| `orgCd` | `ORG_CD` | length=1 | ✓ |
| `slabNo` | `SLAB_NO` | length=20 | ✓ |
| `orderNo` | `ORDER_NO` | length=20 |  |
| `confirmedPlantCd` | `CONFIRMED_PLANT_CD` | length=8 |  |
| `possiblePlantCd` | `POSSIBLE_PLANT_CD` | length=16 |  |
| `slabThickness` | `SLAB_THICKNESS` | precision=6, scale=2 |  |
| `slabWidth` | `SLAB_WIDTH` | precision=10, scale=2 |  |
| `slabWidthHigh` | `SLAB_WIDTH_HIGH` | precision=10, scale=2 |  |
| `slabWidthLow` | `SLAB_WIDTH_LOW` | precision=10, scale=2 |  |
| `slabLength` | `SLAB_LENGTH` | precision=10, scale=2 |  |
| `slabLengthHigh` | `SLAB_LENGTH_HIGH` | precision=10, scale=2 |  |
| `slabLengthLow` | `SLAB_LENGTH_LOW` | precision=10, scale=2 |  |
| `slabWgt` | `SLAB_WGT` | precision=10, scale=3 |  |
| `slabWgtHigh` | `SLAB_WGT_HIGH` | precision=10, scale=3 |  |
| `slabWgtLow` | `SLAB_WGT_LOW` | precision=10, scale=3 |  |
| `slabWidth1` | `SLAB_WIDTH_1` | precision=10, scale=2 |  |
| `slabWidthHigh1` | `SLAB_WIDTH_HIGH_1` | precision=10, scale=2 |  |
| `slabWidthLow1` | `SLAB_WIDTH_LOW_1` | precision=10, scale=2 |  |
| `slabLength1` | `SLAB_LENGTH_1` | precision=10, scale=2 |  |
| `slabLengthHigh1` | `SLAB_LENGTH_HIGH_1` | precision=10, scale=2 |  |
| `slabLengthLow1` | `SLAB_LENGTH_LOW_1` | precision=10, scale=2 |  |
| `slabWgt1` | `SLAB_WGT_1` | precision=10, scale=3 |  |
| `slabWgtHigh1` | `SLAB_WGT_HIGH_1` | precision=10, scale=3 |  |
| `slabWgtLow1` | `SLAB_WGT_LOW_1` | precision=10, scale=3 |  |
| `splitCount` | `SPLIT_COUNT` | — |  |
| `createdAt` | `CREATED_AT` | — |  |

## BusinessTerm 후보 (Phase 2 propose-bindings 입력)

### 1. 핵심 BusinessTerm 시드 (자동 인식 + 컨벤션 보강)

이 항목들은 시나리오 1, 2 시연의 **승인 후 ConceptBinding 의 정답** 입니다.

| BusinessTerm | 정의 | 매칭 컬럼 (테이블.컬럼명) | 일치도 |
|---|---|---|---|
| **품종/품명코드** | 제품 분류 코드. 동일 도메인이지만 4 테이블에서 다른 컬럼명 사용. | cast_spec.PRODUCT_TYPE_CD (length=4) · edging_group.PRODUCT_TYPE_CD (length=4) · hr_spec.PRODUCT_TYPE_CD (length=4) · order_om.PRODUCT_TYPE_CD (length=4) · customer_std.PRODUCT_NAME_CD (length=3) · sd_productivity_std.PROD_KIND_CD (length=4) | ⭐ **시연 victim** (3 변종) |
| **열연공장코드** | 열연 공장 코드 1자리. HR_SPEC 은 HR_PLANT_CD, HR_MIN/MAX_WGT 는 HR_CD 로 표기. | hr_spec.HR_PLANT_CD (length=1) · hr_max_wgt.HR_CD (length=1) · hr_min_wgt.HR_CD (length=1) | ⭐ **시연 victim** (2 변종) |
| **폭 하한** | 설계 가능 폭 하한 값. | cast_spec.WIDTH_LOW · hr_spec.WIDTH_LOW | 정확 매칭 |
| **폭 상한** | 설계 가능 폭 상한 값. | cast_spec.WIDTH_HIGH · hr_spec.WIDTH_HIGH | 정확 매칭 |
| **길이 하한** | 설계 가능 길이 하한 값. | cast_spec.LENGTH_LOW · hr_spec.LENGTH_LOW | 정확 매칭 |
| **길이 상한** | 설계 가능 길이 상한 값. | cast_spec.LENGTH_HIGH · hr_spec.LENGTH_HIGH | 정확 매칭 |
| **포장 단중 하한** | 고객 포장 단위 단중 하한. | customer_std.PKG_WGT_LOW · order_om.PKG_WGT_LOW | 정확 매칭 |
| **포장 단중 상한** | 고객 포장 단위 단중 상한. | customer_std.PKG_WGT_HIGH · order_om.PKG_WGT_HIGH | 정확 매칭 |
| **강종 코드** | 철강 강종 분류 (SS400, SM490, AH36, ...). | edging_group.GRADE_CD (length=10) · order_qd.GRADE_CD (length=10) · sd_productivity_std.GRADE_CD (length=10) | 정확 매칭 |
| **고객사 코드** | 주문 고객사 식별 코드. | customer_std.CUSTOMER_CD (length=10) · edging_group.CUSTOMER_CD (length=10) · order_om.CUSTOMER_CD (length=10) · sd_productivity_std.CUSTOMER_CD (length=10) | 정확 매칭 |
| **온톨로지 코드** | 복합 PK 의 첫 자리 (2자리). | cast_spec.CMP_CD (length=2) · customer_std.CMP_CD (length=2) · edging_group.CMP_CD (length=2) · edging_spec.CMP_CD (length=2) · hr_max_wgt.CMP_CD (length=2) · hr_min_wgt.CMP_CD (length=2) ... +8 | 정확 매칭 |
| **소 코드** | 복합 PK 의 두 번째 자리 (1자리). 온톨로지 내 조직. | cast_spec.ORG_CD (length=1) · customer_std.ORG_CD (length=1) · edging_group.ORG_CD (length=1) · edging_spec.ORG_CD (length=1) · hr_max_wgt.ORG_CD (length=1) · hr_min_wgt.ORG_CD (length=1) ... +8 | 정확 매칭 |
| **주문 번호** | 고객 주문 식별. ORDER_OS = root, 다른 ORDER_* 는 FK→OS. | order_chemical.ORDER_NO (length=20) · order_om.ORDER_NO (length=20) · order_os.ORDER_NO (length=20) · order_qd.ORDER_NO (length=20) · slab_design_hist.ORDER_NO (length=20) · slab_result.ORDER_NO (length=20) | 정확 매칭 |
| **확정 통과 공장 코드** | 8자리 인코딩 — 위치별 공정(SM, HR, HRF, CR, ANL1, ANL2, GAL, CRF), ' '=비활성. | order_os.CONFIRMED_PLANT_CD (length=8) · slab_result.CONFIRMED_PLANT_CD (length=8) | 정확 매칭 |
| **가능 통과 공장 코드** | 16자리 (2자리 × 8 공정) — 갈 수 있는 공장 후보. | order_os.POSSIBLE_PLANT_CD (length=16) · slab_result.POSSIBLE_PLANT_CD (length=16) | 정확 매칭 |
| **우선순위** | 다중 매칭 시 ASC 정렬로 첫 행 선택. EDGING_GROUP / CUSTOMER_STD 에 사용. | customer_std.PRIORITY · edging_group.PRIORITY | 정확 매칭 |
| **두께** | Slab 두께 (mm). 2D sheet (HR_MIN/MAX_WGT) 의 첫 축. | hr_max_wgt.THICKNESS · hr_min_wgt.THICKNESS | 정확 매칭 |
| **폭** | Slab 폭 (mm). 2D sheet 의 두 번째 축. | hr_max_wgt.WIDTH · hr_min_wgt.WIDTH | 정확 매칭 |
| **Slab 설계 두께** | step 1 의 출력. CAST_SPEC 룩업 결과. | cast_spec.SLAB_THICKNESS · slab_result.SLAB_THICKNESS | 정확 매칭 |
| **Slab 번호** | 12자리 zero-padded sequence. SLAB_RESULT/SLAB_DESIGN_HIST PK. | slab_design_hist.SLAB_NO (length=20) · slab_result.SLAB_NO (length=20) | 정확 매칭 |
| **설계 대기량** | 주문 잔여 설계 대상 수량. DG004 cross-check 의 입력. | order_om.DESIGN_PEND_QTY · order_os.DESIGN_PEND_QTY | 정확 매칭 |
| **Edging 그룹 코드** | EDGING_GROUP 룩업 키 → EDGING_SPEC FK. '*' wildcard fallback. | edging_group.EDGING_GROUP_CD (length=10) · edging_spec.EDGING_GROUP_CD (length=10) | 정확 매칭 |

### 2. 시나리오 1·2 victim 컬럼 (도구 시연의 핵심)

#### 품종/품명코드

제품 분류 코드. 동일 도메인이지만 4 테이블에서 다른 컬럼명 사용.

| 테이블 | 컬럼명 | length |
|---|---|---|
| cast_spec | `PRODUCT_TYPE_CD` | 4 |
| edging_group | `PRODUCT_TYPE_CD` | 4 |
| hr_spec | `PRODUCT_TYPE_CD` | 4 |
| order_om | `PRODUCT_TYPE_CD` | 4 |
| customer_std | `PRODUCT_NAME_CD` | 3 |
| sd_productivity_std | `PROD_KIND_CD` | 4 |

**시연 메시지** : 6 컬럼이 도메인적으로 동일하지만 이름·길이가 제각각. 도구가 통합 BusinessTerm 으로 매핑 → 한 컬럼 변경 시 5 컬럼 동시 보정 필요 인사이트.

#### 열연공장코드

열연 공장 코드 1자리. HR_SPEC 은 HR_PLANT_CD, HR_MIN/MAX_WGT 는 HR_CD 로 표기.

| 테이블 | 컬럼명 | length |
|---|---|---|
| hr_spec | `HR_PLANT_CD` | 1 |
| hr_max_wgt | `HR_CD` | 1 |
| hr_min_wgt | `HR_CD` | 1 |

**시연 메시지** : 6 컬럼이 도메인적으로 동일하지만 이름·길이가 제각각. 도구가 통합 BusinessTerm 으로 매핑 → 한 컬럼 변경 시 5 컬럼 동시 보정 필요 인사이트.

## BusinessRule (Javadoc 자동 추출)

`JavadocRuleExtractor` (E1-d) 가 122 java 파일에서 추출한 비즈니스 룰 = 38 건. 한국어 자연어 룰 → CONFLICTS_WITH gap 감지의 코드 쪽 입력.

### CustomerStdJpo

- 🟦 `rule1` : NOTE: PRODUCT_NAME_CD 는 다른 테이블의 PRODUCT_TYPE_CD 와 동일 도메인 개념 (품명/품종).

### CustomerStdPK

- 🟦 `rule1` : (온톨로지, 소구분, 우선순위) — EDGING_GROUP 과 동일한 우선순위 tiebreaker 패턴.

### CustomerStdRepository

- 🟦 `rule1` : (EDGING_GROUP 과 동일한 우선순위 기반 매칭 패턴)

### EdgingGroupRepository

- 🟦 `rule1` : AND HR_TGT_WIDTH_LOW <= ? AND HR_TGT_WIDTH_HIGH >= ?

### HrMaxWgtJpo

- 🟦 `rule1` : THICKNESS >= 입력두께 AND WIDTH >= 입력폭
- 🟦 `rule2` : NOTE: HR_CD 는 HR_SPEC.HR_PLANT_CD 와 동일 도메인 (열연공장 1자리). 레거시 비표준 컬럼명.

### HrMaxWgtRepository

- 🟦 `rule1` : 룩업: 입력 (두께, 폭) → cell.두께 >= 입력두께 AND cell.폭 >= 입력폭 인 row 들 중

### HrMaxWgtService

- 🟦 `rule1` : HrMinWgtService 와 동일한 패턴. 미매칭 시 null. 호출자가 fail 처리 (DG107).

### HrMinWgtJpo

- 🟦 `rule1` : HR_MAX_WGT 와 동일한 룩업 패턴 (사용자 사양 — semantically 특이하지만 사용자 룰 준수).

### HrMinWgtPK

- 🟦 `rule1` : HR_MAX_WGT 와 동일 구조, 다른 의미 (MIN vs MAX).

### HrMinWgtRepository

- 🟦 `rule1` : HR_MAX_WGT 와 동일한 룩업 패턴 (사용자 사양).

### HrMinWgtService

- 🟦 `rule1` : 룩업 패턴: cell.thickness ≥ input.thickness AND cell.width ≥ input.width

### SDOrderOsJpo

- 🟥 `rule1` : feature 레이어에서 직접 접근 금지 — SDOrderLogic 을 통해 SDOrderEntity 로 변환.

### SDOrderOsRepository

- 🟥 `rule1` : feature 직접 호출 금지 — SDOrderLogic 경유.

### SDOrderPK

- 🟦 `rule1` : 필드명·타입은 각 @Entity 의 @Id 필드와 정확히 일치해야 한다 (JPA 규약).

### SDSlabEntity

- 🟦 `rule1` : dev notes 패턴 (SDOrder 와 동일):

### SdFinalLengthRangeAction

- 🟦 `rule1` : slab길이하한 = max((slab단중 / (최종폭상한 × 두께 × 비중)), 1차길이하한)
- 🟦 `rule2` : slab길이상한 = min((slab단중 / (최종폭하한 × 두께 × 비중)), 1차길이상한)

### SdFinalWidthRangeAction

- 🟦 `rule1` : slab폭하한 = ceil(slab단중 / (1차길이상한 × 두께 × 비중)) [1자리 올림]
- 🟦 `rule2` : slab폭상한 = floor(slab단중 / (1차길이하한 × 두께 × 비중)) [1자리 내림]

### SdFirstWeightAction

- 🟦 `rule1` : 단중하한 = 1차두께 × 1차폭하한 × 1차길이하한 × 비중
- 🟦 `rule2` : 단중상한 = 1차두께 × 1차폭상한 × 1차길이상한 × 비중

### SdLengthRangeAction

- 🟦 `rule1` : 길이하한 = max(연주설비하한길이, 열연설비하한길이)
- 🟦 `rule2` : 길이상한 = min(연주설비상한길이, 열연설비상한길이)
- 🟦 `rule3` : 해결: step 2 와 동일한 plant 코드 재사용 + 양쪽 spec 재룩업

### SdMaxSplitCountAction

- 🟦 `rule1` : 산정 결과 ≤ 0 이면 알고리즘 fail (DG108 임시 코드).

### SdOrderValidator

- 🟦 `rule1` : DG003 — 포장단중 range integrity (하/상한 양수 + 하한 ≤ 상한)
- 🟦 `rule2` : DG004 — 설계대기량 양수 + 상한 ≥ 포장단중하한 (cross-table check)
- 🟦 `rule3` : WORK_DUE 가 오늘 날짜와 동일한 경우 통과되어 당일 마감 Slab 생성
- 🟦 `rule4` : 해결: isAfter(today) 로 변경 (>= → >) — 박XX 패치 후 안정화

### SdProductivityStdJpo

- 🟦 `rule1` : NOTE: PROD_KIND_CD 는 다른 테이블의 PRODUCT_TYPE_CD / PRODUCT_NAME_CD 와 동일 도메인 (품명/품종/품종코드).

### SdSlabSaveAction

- 🟦 `rule1` : 원본 (no suffix) + 조정 (_1) 모두 동일 값으로 set (알고리즘 직후 — 추후 사이즈 조정 시 _1 만 갱신)
- 🟦 `rule2` : 모든 row: 동일 도메인값 + 다른 SLAB_NO

### SdTargetWidthAction

- 🟦 `rule1` : 목표slab폭 = (10mm 단위 올림) splitWgtHigh × 1e6 / (최종길이상한 × 두께 × 비중)

### SelectedHrTgtWidthResolver

- 🟥 `rule1` : 비활성(' ') 또는 인식 불가 코드 → null (호출자가 처리 결정)

### checkDesignPendQty

- 🟦 `rule1` : DG004: 설계대기량 양수 + 상한 ≥ 포장단중 하한.

### findGroup

- 🟦 `rule1` : AND HR_TGT_WIDTH_LOW ≤ hrTgtWidth AND HR_TGT_WIDTH_HIGH ≥ hrTgtWidth

### get

- 🟦 `rule1` : 강종별 비중 (현재는 모두 동일, 시나리오 확장용).
